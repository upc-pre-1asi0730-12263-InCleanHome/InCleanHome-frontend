/**
 * Value Object DocumentType — type of document uploaded
 * by an employee during onboarding
 * Layer: IAM / domain / model
 */
export const DocumentType = Object.freeze({
  BACKGROUND_CHECK: "background_check",
  EXPERIENCE: "experience",
});
